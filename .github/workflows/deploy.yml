name: Deploy Airflow DAGs to GCP Compute Machine

on:
  push:
    branches: [main]
    paths:
      - 'dags/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud (optional)
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup gcloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Verify gcloud auth (optional)
        run: |
          gcloud auth list
          gcloud config list project
          echo "✅ gcloud authenticated"

      - name: Upload DAGs to GCE VM via SCP
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.GCE_HOST }}
          username: ${{ secrets.GCE_USER }}
          key: ${{ secrets.GCE_SSH_KEY }}
          source: "dags/"
          target: "/home/${{ secrets.GCE_USER }}/airflow/dags/"
          strip_components: 1

      - name: Restart Airflow on VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GCE_HOST }}
          username: ${{ secrets.GCE_USER }}
          key: ${{ secrets.GCE_SSH_KEY }}
          script: |
            echo "🔄 Restarting Airflow services..."
            sudo systemctl restart airflow-scheduler
            sudo systemctl restart airflow-webserver
            echo "✅ Airflow restarted successfully"
